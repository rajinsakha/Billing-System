export interface IDynamicData{
    
}

export interface TableDataItem {
    headers: string[];
    data: any[]
  }

  export interface TableProps{
    headers:string[];
    data:any[];
    type: string;
  }

